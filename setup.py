# from setuptools import setup
from distutils.core import setup

setup(name='CSV Ploter',
      version='1.0',
      description='GUI Application to plot multiple CSV files',
      url='https://github.com/MarioRaul/CSVPloter',
      author='Mario Freitas',
      author_email='mariofreitas.enc@gmail.com',
      license='MIT',
      packages=[],
      install_requires=[
          'matplotlib'],
      zip_safe=False)
